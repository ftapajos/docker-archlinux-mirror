version: '3'
services:
  mirror-update:
    build: update
    environment:
        - ARCHLINUX_T1_RSYNC_URL
 
    restart: always
    volumes:
      - mirror-data:/mnt/mirror

  mirror-http:
    build: http
    restart: always
    volumes:
      - mirror-data:/mnt/mirror

volumes:
  mirror-data:
